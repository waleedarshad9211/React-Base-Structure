import { Box, Button, Typography } from "@mui/material";

const Login = () => {
  console.log(" Login ");
  return (
    <Box>
      <Typography variant="h1">Login Here to see magic</Typography>
      <Typography variant="h2">Login Here to see magic</Typography>
      <Typography variant="h3">Login Here to see magic</Typography>
      <Typography variant="body1">Login Here to see magic</Typography>
      <Typography variant="body2">Login Here to see magic</Typography>
      <Typography variant="body3">Login Here to see magic</Typography>

      <Button variant="contained">Good to see you</Button>
    </Box>
  );
};

export default Login;
