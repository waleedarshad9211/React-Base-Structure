import { Box, Button, Typography } from "@mui/material";

const AdminPanel = () => {
  console.log(" AdminPanel ");
  return (
    <Box>
      <Typography variant="h1">All is adminpanel</Typography>
      <Typography variant="h2">All is adminpanel</Typography>
      <Typography variant="h3">All is adminpanel</Typography>
      <Typography variant="body1">All is well</Typography>
      <Typography variant="body2">All is well</Typography>
      <Typography variant="body3">All is well</Typography>

      <Button variant="contained">Good to see you</Button>
    </Box>
  );
};

export default AdminPanel;
