import { Box, Button, Typography } from "@mui/material";

const NewPatient = () => {
  console.log(" NewPatient ");
  return (
    <Box>
      <Typography variant="h1">New Patient</Typography>
      <Typography variant="h2">New Patient</Typography>
      <Typography variant="h3">New Patient</Typography>
      <Typography variant="body1">New Patient</Typography>
      <Typography variant="body2">New Patient</Typography>
      <Typography variant="body3">New Patient</Typography>

      <Button variant="contained">Good to see you</Button>
    </Box>
  );
};

export default NewPatient;
